if (age > 12) {
    if (age < 65) {
        ticketPrice = 20;
    } else {
        ticketPrice = 10;
    }
} else {
    ticketPrice = 10;
}

if((age > 12) && (age < 65)) { 
    ticketPrice = 20;
} else {
    ticketPrice = 10;
}